export default function GamesDefault() {
  return (
    <div
      style={{
        height: 100 + "%",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <h1>Please select the genre.</h1>
    </div>
  );
}
